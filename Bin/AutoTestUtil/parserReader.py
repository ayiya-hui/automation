import re

eventType='\<setEventAttribute attr="eventType"\>(P?<eventType>\S+)\</setEventAttribute\>'

def readParser(parserData):
    myExp=re.

if __name__=='__main__':
    myRead=open('parserXml.xml')
    data=myRead.read()
    mRead.close()
    readParser(data)
    print 'task done'

